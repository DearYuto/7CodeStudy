import React from 'react';

import { Playlist } from '@/types';

interface Props {
  title: string;
  subTitme?: string;
  Thumbnail?: React.ReactNode;
  playlistArray?: Playlist[];
}

const PlayListCarousel = () => {
  return <div>PlayListCarousel</div>;
};

export default PlayListCarousel;
